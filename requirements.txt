# AI Intrusion Detection System - Web Version Requirements
# No external dependencies required - uses only Python standard library
# 
# The web version is designed to be completely portable and self-contained
# It only requires Python 3.6+ with standard library modules:
# - http.server (for web server)
# - threading (for background monitoring simulation)
# - json (for data handling)
# - webbrowser (for auto-opening browser)
# - datetime, time, random, os, sys, pathlib, urllib (standard modules)

# No pip install required!
# Just run: python ai_ids_web_portable.py